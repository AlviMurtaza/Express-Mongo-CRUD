h1 Update Note

form(method="POST", action="/notes/#{note.id}")
    input(type="hidden", name="_method", value="PUT")

    p Title:
        input#title(type="text", name="title", value="#{note.title}")
    p Body:
        textarea(name="nbody", value="#{note.nbody}")
    p: button(type="submit") Update

